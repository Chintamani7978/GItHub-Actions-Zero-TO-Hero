name: Docker Build & Push (DISABLED)

# This workflow is disabled - use deploy-app.yml instead
on:
  workflow_dispatch:
    
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "This workflow is disabled"